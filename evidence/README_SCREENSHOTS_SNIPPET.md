## Evidence Screenshots

Selected evidence screenshots are stored in `evidence/screenshots/` and include configuration, 5G core startup, UE registration, tunnel interface checks, connectivity tests, throughput tests, and network impairment results.
